[package]
name = "no-std"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[lib]
path = "lib.rs"

[dependencies]
pin-project-lite = { path = "../.." }

[lints]
workspace = true
